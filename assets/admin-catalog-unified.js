(function(){
  const $=id=>document.getElementById(id);
  const esc=v=>String(v??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]));
  let config=null;

  async function api(url,opts={}){
    const r=await fetch(url,{credentials:'include',cache:'no-store',headers:{'Content-Type':'application/json','Cache-Control':'no-store',...(opts.headers||{})},...opts});
    const d=await r.json().catch(()=>({}));
    if(!r.ok||d.ok===false)throw new Error(d.error||'Erro');
    return d;
  }
  function toast(msg,type='ok'){const el=$('status');if(!el)return;el.textContent=msg;el.className='status '+type;el.classList.remove('hidden')}
  function busy(btn,text='Salvando...'){if(!btn)return()=>{};const old=btn.textContent;btn.textContent=text;btn.disabled=true;return()=>{btn.textContent=old;btn.disabled=false}}
  function lines(v){return String(v||'').split('\n').flatMap(x=>x.split(',')).map(x=>x.trim()).filter(Boolean)}
  function slug(v){return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')}
  function controls(){config.catalogControls=config.catalogControls||{hiddenArtCodes:[],hiddenThemes:[],themeOverrides:[],subthemeOverrides:[],artBlocks:[]};return config.catalogControls}
  function structure(){config.catalogStructure=config.catalogStructure||{collections:[],transplants:[]};config.catalogStructure.collections=config.catalogStructure.collections||[];config.catalogStructure.transplants=config.catalogStructure.transplants||[];return config.catalogStructure}

  async function load(){try{const d=await api('/api/admin/config?ts='+Date.now());config=d.config;controls();structure();renderCatalog();renderUsers();patchOrders();patchSaveButtons();polishAdmin()}catch(e){}}
  async function save(msg,reloadAfter=false){config.ui=config.ui||{};config.ui.cacheVersion=Number(config.ui.cacheVersion||1)+1;await api('/api/admin/config',{method:'POST',body:JSON.stringify({config})});try{await api('/api/admin/cache',{method:'POST'})}catch(_){}toast(msg||'Salvo com sucesso. Catálogo atualizado.');if(reloadAfter)setTimeout(()=>location.reload(),700)}

  function renderCatalog(){
    const p=$('catalogPanel'); if(!p||!config)return;
    const c=controls(); const s=structure();
    p.innerHTML=`
      <div class="card span-12"><div class="sectionHead"><div><h3>Temas e exibição</h3><p>Controle o que aparece para o cliente sem mexer no Drive.</p></div><button id="saveCatalogControl" class="btn green">Salvar e atualizar catálogo</button></div><div class="grid"><div class="field span-6"><label>Artes bloqueadas por código</label><textarea id="hiddenArtCodes" placeholder="2666\n195\n1851">${esc((c.hiddenArtCodes||[]).join('\n'))}</textarea><p class="hint">IDs aqui não aparecem no catálogo nem na busca.</p></div><div class="field span-6"><label>Temas ocultos</label><textarea id="hiddenThemes" placeholder="Naruto\nHeróis">${esc((c.hiddenThemes||[]).join('\n'))}</textarea><p class="hint">Apague o nome daqui e salve para o tema voltar.</p></div></div></div>
      <div class="card span-12"><div class="sectionHead"><div><h3>Organização avançada</h3><p>Coleções virtuais e união de conteúdo. Esta seção é parte nativa da aba, não enxerto.</p></div><button id="refreshCatalogPreview" class="btn secondary" type="button">Atualizar prévia</button></div><div class="grid"><div class="card span-12 softCard"><div class="sectionHead"><div><h3>Coleções de temas</h3><p>Ex.: Disney contendo Minnie e Mickey.</p></div><button id="addCollection" class="btn secondary" type="button">Adicionar coleção</button></div><div id="collectionsList">${(s.collections||[]).map(collectionHtml).join('')||'<p class="hint">Nenhuma coleção criada.</p>'}</div></div><div class="card span-12 softCard"><div class="sectionHead"><div><h3>Unir conteúdo de temas</h3><p>Ex.: Quadrado aparece dentro de Polígonos e Quadrado some da raiz.</p></div><button id="addTransplant" class="btn secondary" type="button">Adicionar união</button></div><div id="transplantsList">${(s.transplants||[]).map(transplantHtml).join('')||'<p class="hint">Nenhuma união criada.</p>'}</div></div><div class="card span-12 softCard"><div class="sectionHead"><div><h3>Prévia real e validações</h3><p>Valida os nomes com o Drive e mostra a estrutura final.</p></div></div><div id="catalogPreviewBody"><p class="hint">Clique em Atualizar prévia para validar antes de salvar.</p></div></div></div></div>
      <div class="card span-12"><div class="sectionHead"><div><h3>Renomear, ocultar e ordenar temas</h3><p>Use o nome original da pasta no Drive.</p></div><button id="addThemeRule" class="btn secondary">Adicionar regra</button></div><div id="themeRules">${(c.themeOverrides||[]).map(ruleHtml).join('')||'<p class="hint">Nenhuma regra criada.</p>'}</div></div>
      <div class="card span-12"><div class="sectionHead"><div><h3>Prévia técnica</h3><p>Resumo do que será salvo.</p></div></div><pre class="jsonBox">${esc(JSON.stringify({catalogControls:c,catalogStructure:s},null,2))}</pre></div>`;
    bindCatalog();
  }

  function bindCatalog(){
    const c=controls(), s=structure();
    $('addThemeRule').onclick=()=>{c.themeOverrides.push({match:'',displayName:'',order:0,hidden:false});renderCatalog()};
    $('addCollection').onclick=()=>{s.collections.push({id:'',name:'Nova coleção',children:[],hideChildrenFromRoot:true,order:0,active:true});renderCatalog()};
    $('addTransplant').onclick=()=>{s.transplants.push({from:'',to:'',mode:'mergeAll',hideSource:true,active:true});renderCatalog()};
    $('refreshCatalogPreview').onclick=loadPreview;
    $('saveCatalogControl').onclick=async e=>{const done=busy(e.currentTarget);try{readCatalogForm();await save('Regras do catálogo salvas.',true)}catch(err){toast(err.message||'Erro ao salvar','err')}finally{done()}};
    document.querySelectorAll('[data-rm-theme-rule]').forEach(b=>b.onclick=()=>{c.themeOverrides.splice(+b.dataset.rmThemeRule,1);renderCatalog()});
    document.querySelectorAll('[data-rm-collection]').forEach(b=>b.onclick=()=>{s.collections.splice(+b.dataset.rmCollection,1);renderCatalog()});
    document.querySelectorAll('[data-rm-transplant]').forEach(b=>b.onclick=()=>{s.transplants.splice(+b.dataset.rmTransplant,1);renderCatalog()});
  }

  function readCatalogForm(){
    const c=controls(), s=structure();
    c.hiddenArtCodes=lines($('hiddenArtCodes').value).map(x=>x.replace(/\D/g,'')).filter(Boolean);
    c.hiddenThemes=lines($('hiddenThemes').value);
    c.themeOverrides=[];
    document.querySelectorAll('[data-theme-rule]').forEach(row=>{const r={match:row.querySelector('[data-r="match"]').value.trim(),displayName:row.querySelector('[data-r="displayName"]').value.trim(),order:Number(row.querySelector('[data-r="order"]').value)||0,hidden:row.querySelector('[data-r="hidden"]').value==='true'};if(r.match||r.displayName)c.themeOverrides.push(r)});
    s.collections=[];
    document.querySelectorAll('[data-collection-row]').forEach(row=>{const name=row.querySelector('[data-coll="name"]').value.trim();const children=lines(row.querySelector('[data-coll="children"]').value);if(name&&children.length)s.collections.push({id:row.querySelector('[data-coll="id"]').value.trim()||slug(name),name,children,hideChildrenFromRoot:row.querySelector('[data-coll="hideChildrenFromRoot"]').value==='true',order:Number(row.querySelector('[data-coll="order"]').value)||0,active:row.querySelector('[data-coll="active"]').value==='true'})});
    s.transplants=[];
    document.querySelectorAll('[data-transplant-row]').forEach(row=>{const from=row.querySelector('[data-tr="from"]').value.trim();const to=row.querySelector('[data-tr="to"]').value.trim();if(from&&to)s.transplants.push({from,to,mode:row.querySelector('[data-tr="mode"]').value,hideSource:row.querySelector('[data-tr="hideSource"]').value==='true',active:row.querySelector('[data-tr="active"]').value==='true'})});
    return {catalogControls:c,catalogStructure:s};
  }

  async function loadPreview(){const body=$('catalogPreviewBody'), btn=$('refreshCatalogPreview');if(!body||!btn)return;const done=busy(btn,'Validando...');body.innerHTML='<p class="hint">Lendo Drive...</p>';try{const draft=readCatalogForm();const data=await api('/api/admin/catalog-preview',{method:'POST',body:JSON.stringify({catalogStructure:draft.catalogStructure})});renderPreview(data)}catch(e){body.innerHTML=`<div class="previewWarn">${esc(e.message||'Erro')}</div>`}finally{done()}}
  function renderPreview(data){const body=$('catalogPreviewBody');const warnings=data.warnings||[], info=data.info||[], tree=data.tree||[];body.innerHTML=`<div class="catalogPreviewSummary"><b>${warnings.length?warnings.length+' alerta(s)':'Nenhum alerta'}</b><span>${data.totalThemes||0} tema(s) no Drive · ${tree.length} item(ns) na raiz final</span></div>${warnings.map(w=>`<div class="previewWarn">⚠ ${esc(w)}</div>`).join('')}${info.map(i=>`<div class="previewOk">✓ ${esc(i)}</div>`).join('')}<div class="catalogTree">${tree.map(nodeHtml).join('')}</div>`}
  function nodeHtml(n){if(n.type==='collection')return `<div class="treeNode collection"><b>📁 ${esc(n.label)}</b><small>${esc(n.status||'Coleção')}</small>${(n.children||[]).map(c=>`<p class="treeChild ${c.exists?'':'missing'}">${c.exists?'├':'⚠'} ${esc(c.label)}${c.exists?'':' · não encontrado'}</p>`).join('')}</div>`;return `<div class="treeNode"><b>${esc(n.label)}</b><small>${esc(n.status||'Tema')}</small>${(n.incoming||[]).map(i=>`<p class="treeChild ${i.exists?'':'missing'}">↳ recebe ${esc(i.from)}${i.exists?'':' · doador não encontrado'}</p>`).join('')}</div>`}

  function collectionHtml(r,i){return `<div class="item" data-collection-row="${i}"><div class="grid"><div class="field span-3"><label>Nome da coleção</label><input data-coll="name" value="${esc(r.name||'')}"></div><div class="field span-2"><label>ID interno</label><input data-coll="id" value="${esc(r.id||'')}"></div><div class="field span-4"><label>Temas dentro</label><textarea data-coll="children" placeholder="Minnie\nMickey">${esc((r.children||[]).join('\n'))}</textarea></div><div class="field span-1"><label>Ordem</label><input data-coll="order" value="${esc(r.order||0)}"></div><div class="field span-1"><label>Ocultar filhos</label><select data-coll="hideChildrenFromRoot"><option value="true" ${r.hideChildrenFromRoot!==false?'selected':''}>Sim</option><option value="false" ${r.hideChildrenFromRoot===false?'selected':''}>Não</option></select></div><div class="field span-1"><label>Status</label><select data-coll="active"><option value="true" ${r.active!==false?'selected':''}>Ativa</option><option value="false" ${r.active===false?'selected':''}>Inativa</option></select></div><div class="field span-12"><button class="btn danger" data-rm-collection="${i}" type="button">Remover coleção</button></div></div></div>`}
  function transplantHtml(r,i){return `<div class="item" data-transplant-row="${i}"><div class="grid"><div class="field span-3"><label>Tema doador</label><input data-tr="from" value="${esc(r.from||'')}"></div><div class="field span-3"><label>Tema receptor</label><input data-tr="to" value="${esc(r.to||'')}"></div><div class="field span-2"><label>Tipo</label><select data-tr="mode"><option value="mergeAll" ${r.mode!=='mergeItems'&&r.mode!=='mergeFolders'?'selected':''}>Juntar tudo</option><option value="mergeItems" ${r.mode==='mergeItems'?'selected':''}>Só artes diretas</option><option value="mergeFolders" ${r.mode==='mergeFolders'?'selected':''}>Só subpastas</option></select></div><div class="field span-2"><label>Ocultar doador</label><select data-tr="hideSource"><option value="true" ${r.hideSource!==false?'selected':''}>Sim</option><option value="false" ${r.hideSource===false?'selected':''}>Não</option></select></div><div class="field span-1"><label>Status</label><select data-tr="active"><option value="true" ${r.active!==false?'selected':''}>Ativa</option><option value="false" ${r.active===false?'selected':''}>Inativa</option></select></div><div class="field span-1"><label>Ação</label><button class="btn danger" data-rm-transplant="${i}" type="button">Remover</button></div></div></div>`}
  function ruleHtml(r,i){return `<div class="item" data-theme-rule="${i}"><div class="grid"><div class="field span-3"><label>Tema original</label><input data-r="match" value="${esc(r.match||'')}"></div><div class="field span-3"><label>Nome exibido</label><input data-r="displayName" value="${esc(r.displayName||'')}"></div><div class="field span-2"><label>Ordem</label><input data-r="order" value="${esc(r.order||0)}"></div><div class="field span-2"><label>Oculto</label><select data-r="hidden"><option value="false" ${!r.hidden?'selected':''}>Não</option><option value="true" ${r.hidden?'selected':''}>Sim</option></select></div><div class="field span-2"><label>Ação</label><button class="btn danger" data-rm-theme-rule="${i}" type="button">Remover</button></div></div></div>`}

  function renderUsers(){const p=$('usersPanel')||$('permissionsPanel');if(!p||!config)return;config.permissions=config.permissions||{users:[],roles:['admin','vendedora','editor','visualizador']};const users=config.permissions.users||[];p.innerHTML=`<div class="card span-12"><div class="sectionHead"><div><h3>Usuários e permissões</h3><p>Cadastro administrativo interno. O acesso principal por ADMIN_SECRET_KEY continua ativo.</p></div><button id="addUser" class="btn secondary">Adicionar usuário</button></div><div id="usersList">${users.map(userHtml).join('')||'<p class="hint">Nenhum usuário criado.</p>'}</div><button id="saveUsers" class="btn green">Salvar usuários</button><p class="hint">Por segurança, não coloque senhas reais enquanto a autenticação individual não estiver totalmente ativada no backend.</p></div>`;$('addUser').onclick=()=>{users.push({username:'novo',name:'Novo usuário',role:'editor',active:true});renderUsers()};$('saveUsers').onclick=async e=>{const done=busy(e.currentTarget);try{document.querySelectorAll('[data-user-row]').forEach(row=>{const i=+row.dataset.userRow;users[i]={username:row.querySelector('[data-u="username"]').value.trim(),name:row.querySelector('[data-u="name"]').value.trim(),role:row.querySelector('[data-u="role"]').value,active:row.querySelector('[data-u="active"]').value==='true'}});config.permissions.users=users.filter(u=>u.username);await save('Usuários salvos.',true)}catch(err){toast(err.message||'Erro ao salvar','err')}finally{done()}};document.querySelectorAll('[data-rm-user]').forEach(b=>b.onclick=()=>{users.splice(+b.dataset.rmUser,1);renderUsers()})}
  function userHtml(u,i){const roles=(config.permissions.roles||['admin','vendedora','editor','visualizador']);return `<div class="item" data-user-row="${i}"><div class="grid"><div class="field span-3"><label>Usuário</label><input data-u="username" value="${esc(u.username||'')}"></div><div class="field span-3"><label>Nome</label><input data-u="name" value="${esc(u.name||'')}"></div><div class="field span-2"><label>Permissão</label><select data-u="role">${roles.map(r=>`<option ${u.role===r?'selected':''}>${esc(r)}</option>`).join('')}</select></div><div class="field span-2"><label>Status</label><select data-u="active"><option value="true" ${u.active!==false?'selected':''}>Ativo</option><option value="false" ${u.active===false?'selected':''}>Inativo</option></select></div><div class="field span-2"><label>Ação</label><button class="btn danger" data-rm-user="${i}" type="button">Remover</button></div></div></div>`}

  function patchOrders(){if(document.body.dataset.ordersDeletePatched)return;document.body.dataset.ordersDeletePatched='1';document.addEventListener('click',async e=>{const btn=e.target&&e.target.closest('[data-delete-order]');if(!btn)return;const id=btn.dataset.deleteOrder;if(!confirm('Excluir este pedido?'))return;const done=busy(btn,'Excluindo...');try{const d=await api('/api/orders/delete',{method:'POST',body:JSON.stringify({id})});toast(d.alreadyMissing?'Pedido já não existia no banco. Removi da tela.':'Pedido excluído.');btn.closest('.item')?.remove()}catch(err){toast(err.message,'err')}finally{done()}})}
  function enhanceOrdersPanel(){const list=$('ordersList');if(!list)return;list.querySelectorAll('.item').forEach(item=>{if(item.querySelector('[data-delete-order]'))return;const sel=item.querySelector('[data-order-status]');const id=sel&&sel.dataset.orderStatus;if(!id)return;const wrap=document.createElement('div');wrap.className='actions orderActions';wrap.innerHTML=`<button class="btn danger" data-delete-order="${esc(id)}" type="button">Excluir pedido</button>`;item.appendChild(wrap)})}
  function patchSaveButtons(){['saveBtn','saveBtnTop'].forEach(id=>{const b=$(id);if(!b||b.dataset.progressPatched)return;b.dataset.progressPatched='1';b.addEventListener('click',()=>{const old=b.textContent;b.textContent='Salvando...';b.disabled=true;setTimeout(()=>{b.textContent=old;b.disabled=false},2200)},true)})}
  function polishAdmin(){document.querySelectorAll('#permissionsPanel > .card,.softCard').forEach(card=>card.classList.add('softCard'))}
  function injectStyle(){if($('catalogUnifiedStyle'))return;const s=document.createElement('style');s.id='catalogUnifiedStyle';s.textContent='.catalogPreviewSummary{display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:12px}.previewWarn,.previewOk{border-radius:14px;padding:10px 12px;margin:7px 0;font-weight:900}.previewWarn{background:#fff1f1;color:#a52222}.previewOk{background:#eefbf6;color:#116b52}.catalogTree{display:grid;gap:10px;margin-top:12px}.treeNode{border:1px solid #eee2e4;background:#fff;border-radius:18px;padding:13px}.treeNode.collection{background:#fff8fb;border-color:#ffd6e5}.treeNode b{display:block;font-family:Montserrat}.treeNode small{color:#746d73;font-weight:900}.treeChild{margin:8px 0 0;padding:8px 10px;border-radius:12px;background:#f8f5f3;font-weight:800}.treeChild.missing{background:#fff1f1;color:#a52222}.softCard{border:1px solid #eee2e4!important;border-radius:18px!important;background:#fff!important}';document.head.appendChild(s)}

  new MutationObserver(()=>{enhanceOrdersPanel();polishAdmin()}).observe(document.body,{childList:true,subtree:true});
  document.addEventListener('click',e=>{const tab=e.target&&e.target.closest('[data-tab]');if(!tab)return;setTimeout(()=>{if(tab.dataset.tab==='catalogView')renderCatalog();if(tab.dataset.tab==='permissionsView')renderUsers();enhanceOrdersPanel();patchSaveButtons();polishAdmin()},250)});
  injectStyle();
  setTimeout(load,900);
})();
